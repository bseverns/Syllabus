# Plugin lane (JUCE) — optional

This repo stays lightweight; build JUCE projects locally.

Checklist:
- parameters mirror `dsp/params` (names, ranges, units)
- no allocations in `processBlock`
- automation + preset recall tested
- CPU profiled
