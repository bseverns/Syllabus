# tests/

Put small, repeatable checks here.
Keep them runnable by a stranger.

- `tests/audio/` (if audio exists)
- `tests/firmware/` (serial logs, timing)
- `tests/interaction/` (event logs, replays)
