# The promise (template)

In one paragraph:
- what this tool guarantees
- what it refuses to do
- what happens when it fails

Then list 3 measurable commitments (examples):
- latency stays under X ms under normal load
- knob changes do not zipper
- safe mode always works
