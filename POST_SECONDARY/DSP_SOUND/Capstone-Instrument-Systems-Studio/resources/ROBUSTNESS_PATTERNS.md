# Robustness patterns

- safe mode on boot (hardware gesture)
- factory reset that works offline
- defaults that are musical
- diagnostics: INFO, SELFTEST, VERSION
- loss policies: decide what you drop under overload
- log enough to debug, not enough to spam
