# Test procedures (template)

Define 3–6 tests that are repeatable.

Examples:
- firmware loop timing log + jitter stats
- MIDI rate cap test (no spam)
- audio render A/B + peak/RMS/DC check
- sensor dropout test (installation lane)

For each test:
- Setup
- Steps
- Expected result
- What to do if it fails
