# HW 06 — Serial protocol contract

1. Document every supplied command/message with frame boundary, fields, type/range, example, acknowledgment, and error.
2. Capture a transcript containing `PING`, `GET`, valid `LED`/`RATE`, one invalid command, and one out-of-range value.
3. Design one additional command on paper; include malformed and unsafe inputs before implementing it.
4. Ask a peer to use the protocol document without reading the firmware and record the first ambiguity.

Submit protocol table, redacted transcript, firmware or design extension, test note, and `reflections/week06.md`.
