# Submission Guide

Each week, students commit:
- firmware sketch folder for that week (with notes)
- any wiring notes in `hardware/` (photo + description)
- lab notebook outputs saved
- reflection: `reflections/weekXX.md`

Use `export/` for CSV logs and plot images.
