# Contributing
- Keep lab‑specific slicer recipes in `slicer-profiles/*/local/` (git‑ignored).
- Document any machine changes (nozzle, build plate, firmware) in `checklists/` notes.
- When adding G‑code tricks, include a short rationale and a safe rollback.
- Remove PII from logs/photos before sharing.
