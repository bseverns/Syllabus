# MakerBot SKETCH Notes (Dual‑Fleet)

- Slice with MakerBot’s software to produce the printer‑specific job package.
- Mirror settings from the **parity matrix** (`docs/profiles-parity-matrix.csv`) so results match LulzBot as closely as practical.
- For pauses/inserts, use the MakerBot UI tools rather than manual G‑code edits.
- Use the naming schema in `docs/dual-fleet-pipeline.md` to keep paired builds together.
