# Handheld Scanning — Best Practices

- Calibrate; track drift; use markers if supported.
- Keep steady distance/angle; slow arcs; avoid shiny/transparent surfaces.
- Immediately inspect for holes; re‑scan problem regions.
- Export watertight mesh; keep separate a decimated version for slicing.
