# Contributing
- Keep printer‑specific profiles in `slicer-profiles/*/local/` (git‑ignored).
- Add previews for new mechanism templates (`.png` renders) when practical.
- Note any rig parameter changes in `activities/rigs/README.md`.
- Remove PII from photos/logs before sharing.
