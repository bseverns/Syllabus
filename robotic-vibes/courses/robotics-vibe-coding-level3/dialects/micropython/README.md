# MicroPython / CircuitPython — Level 3 (ESP32 recommended)

- **uasyncio** tasks: comms, control, UI.
- ESP‑NOW or Wi‑Fi UDP for packets; OSC via UDP to laptop.
- Optional: overhead cam→pose stream → correction loop.
- Milestones: sync → localization loop → scenes → performance.
